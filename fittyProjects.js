fitty('.projectTitle', {
    minSize: 12,
    maxSize: 15,
    multiLine: false
  });
  
fitty('.projectDesc', {
    minSize: 10,
    maxSize: 12
  });
  
function refreshFitty() {
    fitty('.projectTitle', {
        minSize: 12,
        maxSize: 15,
        multiLine: false
    });
    fitty('.projectDesc', {
        minSize: 10,
        maxSize: 12
    });
}
//#uploadProjectsTitle
//.projectTitle
