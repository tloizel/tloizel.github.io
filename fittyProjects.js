fitty('.projectTitle', {
    minSize: 14,
    maxSize: 16,
    multiLine: false
  });
  
fitty('.projectDesc', {
    minSize: 12,
    maxSize: 13
  });
  
function refreshFitty() {
    fitty('.projectTitle', {
        minSize: 14,
        maxSize: 16,
        multiLine: false
    });
    fitty('.projectDesc', {
        minSize: 12,
        maxSize: 13
    });
}
//#uploadProjectsTitle
//.projectTitle
